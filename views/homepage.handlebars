{{!-- devices section --}}
<div class="container">
    <div class="cardContainer d-flex flex-row flex-wrap">
        {{#each devices as |device| }}
            {{> device-details }}
        {{/each}}

    </div>
</div>